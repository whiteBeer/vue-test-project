export enum CircularProgressState {
  inProgress = 'inProgress',
  success = 'success',
  warning = 'warning',
  error = 'error',
}

export enum CircularProgressType {
  circle = 'circle',
  dashboard = 'dashboard',
}
